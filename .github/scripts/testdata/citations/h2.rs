// Root-level digit-bearing fixture, deliberately only three lines long:
// `mod/LIFECYCLE.md` cites line 8 of its OWN sibling `h2.rs`, which resolves
// only because a cited path binds to the citing document's directory first.
