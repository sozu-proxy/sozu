# Broken fixture changelog -- its one test citation must be reported

- **`fix(fixture)`: a changelog entry cites its evidence by test name.**
  `a_fixture_changelog_test_name_that_names_no_function` is cited here as the
  test that pinned the defect, and names no function in the fixture tree.
